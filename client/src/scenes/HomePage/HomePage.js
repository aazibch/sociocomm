import { Box } from '@mui/material';
import Navbar from '../Navbar/Navbar';

const HomePage = () => {
    return (
        <Box>
            <Navbar />
        </Box>
    );
};

export default HomePage;
